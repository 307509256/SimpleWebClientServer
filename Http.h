#ifndef __HTTP__
#define __HTTP__

#include <sting>
#include <vector>

class Http
{
protected:
	//common elements
	char[8] protocolVersion;

public:
	//get
	//virtual char* getProtocolVersion();

	//set
	//virtual void setProtocolVersion(char* &serVal);
};

#endif